import { NavLink, Outlet, Link } from 'react-router-dom'
import { useCart } from '../features/cart/cart-context'
import { useStorefront } from '../features/storefront/storefront-context'
import { CartDrawer } from '../features/cart/CartDrawer'

export function PublicLayout() { const cart = useCart(); const { data } = useStorefront(); const name = data?.business.tradeName ?? 'Distrito Smash'; return <div className="site-shell"><header className="site-header"><Link className="wordmark" to="/"><span>DISTRITO</span>SMASH</Link><nav className="desktop-nav" aria-label="Navegación principal"><NavLink to="/" end>Inicio</NavLink><NavLink to="/menu">Menú</NavLink><a href="/#promociones">Promociones</a><a href="/#ubicacion">Ubicación</a></nav><div className="header-actions"><button className="cart-trigger" onClick={cart.open} aria-label={`Abrir pedido, ${cart.units} unidades`}>Pedido <span>{cart.units}</span></button><Link className="button button--acid header-cta" to="/menu">Pedir ahora</Link></div></header><Outlet /><footer className="site-footer"><div><Link className="wordmark" to="/"><span>DISTRITO</span>SMASH</Link><p>{data?.business.description ?? 'Fuego, barrio y hamburguesas hechas sin atajos.'}</p></div><div><small>Explora</small><Link to="/menu">Menú</Link><a href="/#promociones">Promociones</a><a href="/#ubicacion">Visítanos</a></div><div><small>Contacto</small><a href={`tel:${data?.business.phone ?? ''}`}>{data?.business.phone ?? '—'}</a><a href={data?.business.instagram ?? '#'}>Instagram</a></div><p className="powered">Operado con ServiFood</p></footer><nav className="mobile-nav" aria-label="Navegación móvil"><NavLink to="/" end><span>⌂</span>Inicio</NavLink><NavLink to="/menu"><span>☰</span>Menú</NavLink><button onClick={cart.open}><span className="mobile-cart-count">{cart.units}</span>Pedido</button></nav><CartDrawer /><span className="sr-only">Restaurante actual: {name}</span></div> }
